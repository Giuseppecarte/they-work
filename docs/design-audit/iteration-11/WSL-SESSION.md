# VS Code + Ubuntu WSL preview session

Status: **not tested** — fill this record from the actual WSL terminal.

- Date:
- Preview source revision (`PREVIEW.json`):
- Archive and SHA256:
- VS Code version and commit (Help → About):
- Remote indicator:
- Ubuntu version (`cat /etc/os-release`):
- WSL kernel (`uname -r`):
- Architecture (`uname -m`):
- Terminal font and size:
- `stty size` before launch (rows, columns):
- Image / GPU / custom glyph settings:
- `NO_COLOR` present? Invocation used:
- Doctor `terminal_color`, `terminal_graphics`, `cell_pixels`, `terminal_frame`:

| Check | Result: pass / defect / not tested | Evidence or reproduction |
| --- | --- | --- |
| Complete office colors and readable faces | not tested | |
| Image detection / native fallback | not tested | |
| Click worker, computer and nameplate | not tested | |
| Keyboard selection and Tab / Shift+Tab | not tested | |
| Request visible; exact recipient clear | not tested | |
| Recorded demo request is distinguishable from answerable request | not tested | |
| Inspecting connected request does not approve | not tested | Authenticated exercise is separate from the demo |
| Preset and all four office palettes | not tested | |
| Resize and retained worker / request | not tested | |
| Light, 256-color, monochrome | not tested | |
| Reduced motion | not tested | |
| Quit restores shell and mouse | not tested | |
| Real sources: observed vs controllable | not tested | |
| Input-to-visible latency | not tested | Requires timed recording, not subjective estimate |

Complete screenshots / recording paths:

Most useful change:

Most confusing or unfinished detail:
